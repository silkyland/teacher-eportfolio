# ฟอนต์ TH Sarabun New (SIPA)

ไฟล์ฟอนต์ในโฟลเดอร์นี้ใช้สำหรับส่งออก PDF เท่านั้น

หลัง clone โปรเจกต์ ให้รันคำสั่ง:

```bash
php artisan pdf:install-fonts
```

หรือใน Docker:

```bash
docker compose exec app php artisan pdf:install-fonts
```
